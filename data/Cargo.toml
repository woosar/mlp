[package]
name = "data"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
insta = "1.47.2"

[dependencies]
network = {path = "../network"}
rand = "0.10.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"

[lints]
workspace = true
